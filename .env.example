# Copy this file to ".env.local" (which is git-ignored) and set your own values.
# For Vercel/Netlify, add the same variables in the project's Environment Variables.

# Passphrase to unlock the /admin editor.
VITE_ADMIN_PASS=change-me-to-something-secret

# (Optional, for the future "live for everyone" backend — ask Claude to wire it.)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
